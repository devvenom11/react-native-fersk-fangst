/** @module src/market/profile/ProfileImage.styles */

import { StyleSheet } from 'react-native';

/**
 * Stylesheet for ProfileImage component.
 * @type {StyleSheet}
 */
const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
    borderRadius: 45,
    justifyContent: 'center',
    height: 65,
    width: 65,
    // backgroundColor: 'gray'
  }
});


export default styles;
