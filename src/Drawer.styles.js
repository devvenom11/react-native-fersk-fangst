/** @module src/Drawer.styles */

import { StyleSheet } from 'react-native';

/**
 * Stylesheet for Drawer component.
 * @type {StyleSheet}
 */
const styles = StyleSheet.create({
  container: {
    flex: 1
  },
  routes: {
    paddingBottom: 10,
    paddingTop: 20
  },
  footer: {
    marginTop: 30
  }
});

export default styles;
