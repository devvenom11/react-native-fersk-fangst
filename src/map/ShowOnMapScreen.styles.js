/** @module src/map/ShowOnMap.styles */

import { StyleSheet } from 'react-native';

/**
 * Stylesheet for ShowOnMap.
 * @type {StyleSheet}
 */
const styles = StyleSheet.create({
  map: {
    flex: 1
  }
});

export default styles;
